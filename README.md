# OrbitEngine

OrbitEngine is a standalone astronomical simulation engine intended to provide physically plausible object positioning, orbit propagation, trajectory calculation, and reference-frame handling behind a TypeScript API.

The project is designed for a TypeScript/Node.js consumer API with performance-critical capabilities implemented in a portable C++ core that can target both a native Node.js addon and WebAssembly.
